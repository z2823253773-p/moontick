# T5 首次远程 CI 与权限修复证据

日期：2026-09-22。公开仓库：<https://github.com/z2823253773-p/moontick>。

## 首轮真实失败

- 固定 SHA：`e47e5ac8e45ec0c4ecb19f6ac988b0125ec3d1a1`
- [run 35746584237](https://github.com/z2823253773-p/moontick/actions/runs/35746584237)：macOS、Linux 均 failure。
- 两平台通过 checkout、架构断言、固定归档下载与发行方二进制 sidecar 比对，均在 `Assert pinned compiler version` 停止：`bin/moon: Permission denied`；后续 native 测试未执行。
- 本机留存的官方 macOS 归档 `tar -tvf` 显示 `bin/moon`、`bin/moonc` 原始模式为 `-rw-rw-r--`；在隔离目录解压后均为 `0644`、不可执行。补执行位后，同一 `moonc -v` 返回 `v0.10.14+7d59c7ec9 (2026-09-18)`。

## 单点修复及复验

- 修复 SHA：`e047c4d20db6b83db7e633730b07f9467045201d`。只改 `.github/workflows/ci.yml`：二进制归档解压后，对 `bin/` 下非 `.wasm` 文件补执行位。未改变版本、归档哈希、runner、core 说明、任何产品代码或测试。
- 修复前已用原归档复现不可执行；修复命令在隔离解压目录执行后，`moon`、`moonc` 可执行，`moonc -v` 返回预期版本。
- 本地 `git diff --check` 通过；Ruby YAML 解析通过；13 段 `run:` 脚本的 `bash -n` 全部通过。Python `yaml` 模块在当前本机解释器中不存在，故未声称 PyYAML 检查通过。
- [run 35747020671](https://github.com/z2823253773-p/moontick/actions/runs/35747020671)：固定 HEAD 为上述修复 SHA，整体 `completed / success`。两个作业均 `completed / success`： [macOS arm64](https://github.com/z2823253773-p/moontick/actions/runs/35747020671/job/106811152980)、[Linux x86_64](https://github.com/z2823253773-p/moontick/actions/runs/35747020671/job/106811152672)。
- 两平台日志均显示 `moonc reports: v0.10.14+7d59c7ec9 (2026-09-18)`；发行方 sidecar 校验的二进制 SHA-256 分别为 macOS `20967f9389ac54508899ee2fc051a3470d051452bac5818a9696ed4c144f3ec3`、Linux `9226694de9ff978db1ecf820b7710c4224e84ec7a76b19a222d96f0cd4e31b6a`。
- 两平台均运行 native format/check/build/test；`moon test` 各为 `Total tests: 77, passed: 77, failed: 0`，真实 CLI 各为 `Ran 42 tests` 且作业通过；独立 oracle 各输出 `manual=8 random=1000 seed=20260920 metamorphic=30x3 large_N=10^12`，其步骤通过。

## 边界

- 官方 core 归档的 `.sha256` sidecar 仍无可用发行方证明；Linux 侧为 `UNKNOWN_NOT_VERIFIED`。实际 core SHA 作为观察值写入 job summary，这不构成供应链验证通过。
- Actions 的 `actions/checkout@v4` 有 Node.js 20 弃用警告；两个作业成功，警告未被写成零警告。
- 此运行仅证明上述 SHA 与固定工具链在两个 runner 上通过；Mooncakes 发布、赛事申报、验收及季度评选均尚无完成证据。
