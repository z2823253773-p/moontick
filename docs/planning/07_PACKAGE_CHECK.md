# v1.1 工作包校验

日期：2026-09-21。本页记录文档与契约样例校验，不代表产品测试。

状态：PASS（仅以下文档检查范围）。[机器记录](evidence/G0-R/planning-validation.json)。

- 17 份 Markdown：围栏成对、无修订占位符；29 处相对链接目标存在。
- 2 段 Python 示例经 AST 语法检查；1 段报告 JSON 可解析。没有执行产品 CLI 示例。
- 7 行核心边界表由集合枚举 oracle 独立核算计数。
- 3 个合成场景的计数、缺失区间和最长缺口与枚举结果一致。
- 报告示例的计数及缺口合计成立；无表头格式的重复行=3、离网格行=5。
- 原 v1 的 SHA256SUMS 全部匹配；产品目录 moontick 不存在。

封装包含 SHA256SUMS.txt；zip 完成后逐项读取内容并核对哈希。清单不包含它自身。产品构建/测试/CI/发布、上游系统复现均 NOT_RUN。
