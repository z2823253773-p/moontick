# Codex：G0-R 接力与独立验收

```text
我要以 MoonTick 参加季度评选。Claude Code 主实现，你负责契约和独立验证。
工作包：/Users/henryz/Desktop/比赛/MoonTick-季度参赛方案-v1.1。换机器定位同名目录。
先读 00、08、09、10，再读当前任务及相关规格。

不要从零重做选题。三项目固定版本对照来自我提供的 G0，两条需求来源已核对；真实 MoonTick 接入为零。v0.1 是纯库 + ticks，不做通用 CSV。

当前工具链可重装性未决：候选历史发行物四个 HEAD 返回 403。核实发行物与固定方式，记录输出；403 不证明版本不存在。继续只读调查与规划；未得实施指令前不建产品仓库、不改全局工具链或写产品。

若我已明确让你按 v1.1 开工，则记录授权，不重复询问；独立建库，由 Claude 执行 T1。最小工程验证配置格式和 native；版本固定未解决则保留 CI 阻断，不假装通过。

产品路径 /Users/henryz/Desktop/比赛/moontick，先确认无未知内容；父目录不得 git init。规划复制 docs/planning，模板落根目录/.ai；之后仓库内规划为唯一有效版本。

审查 Claude 的明确 SHA；独立 oracle 用小网格枚举，不复制产品算法。核对空输入、重复不补覆盖、Int64、超大缺口、行号和真实退出码；失败给最小反例。

规则问题由我发送；不代发消息或报名。每轮落盘状态，不伪造测试、commit、用户与回执。
```
