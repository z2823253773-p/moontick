// Mooncakes namespace was verified through the isolated `moon login` session
// on 2026-09-21. Publishing requires a separate release decision.
name = "z2823253773-p/moontick"

version = "0.1.0"

readme = "README.md"

repository = "https://github.com/z2823253773-p/moontick"

license = "MIT"

keywords = [ "testing", "time-series", "sample-plan", "audit" ]

preferred_target = "native"

description = "Audit a fixed-interval sampling plan against observed tick timestamps."
